---
import ServiceLayout from '../../layouts/ServiceLayout.astro';
import servicesData from '../../data/services.json';

const service = servicesData.services.find(s => s.id === 'workflow-audit')!;
---

<ServiceLayout
  title={service.title}
  description={service.fullDescription}
  serviceId={service.id}
  priceRange={service.priceRange}
  faqs={service.faqs}
  ctaText={service.ctaText}
  ctaHref={service.ctaHref}
>
  <!-- Hero with Direct Answer (50-70 words) -->
  <header class="mb-12 scroll-animate">
    <span class="badge badge-accent mb-4">{service.priceRange}</span>
    <h1 class="mb-4">{service.title}</h1>
    <p class="text-xl text-gray-300">
      A Workflow Audit is a focused review of your current operations with specific, actionable recommendations. I analyze your existing tools, workflows, and pain points, then deliver a written audit report with a prioritized action list. Includes a 30-minute call to walk through findings. Delivered within one week for $500.
    </p>
  </header>

  <!-- What's Included -->
  <section class="mb-12 scroll-animate">
    <h2 class="mb-6">What's Included</h2>
    <ul class="space-y-4">
      {service.included.map((item) => (
        <li class="flex items-start gap-3">
          <svg class="w-6 h-6 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
          <span class="text-gray-300">{item}</span>
        </li>
      ))}
    </ul>
  </section>

  <!-- Who This Is For -->
  <section class="mb-12 glass-card rounded-2xl p-8 scroll-animate">
    <h2 class="mb-4 text-white">Who This Is For</h2>
    <p class="text-gray-300 mb-4">
      A Workflow Audit is right for you if:
    </p>
    <ul class="space-y-3 text-gray-300">
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>You want expert feedback before committing to a larger investment</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>You're not sure if your current systems are as broken as they feel</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>You need a clear starting point—what to fix first</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>You prefer to DIY but want professional guidance on where to focus</span>
      </li>
    </ul>
  </section>

  <!-- Process -->
  <section class="mb-12 scroll-animate">
    <h2 class="mb-8">How It Works</h2>
    <div class="space-y-6">
      {service.process.map((phase) => (
        <div class="flex gap-6 items-start">
          <div class="flex-shrink-0">
            <div class="w-10 h-10 rounded-full bg-accent-500/20 text-accent-400 flex items-center justify-center font-bold">
              {phase.step}
            </div>
          </div>
          <div class="pt-1">
            <p class="text-gray-300">{phase.description}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- What's In The Report -->
  <section class="mb-12 glass-card rounded-2xl p-8 border border-accent-500/20 scroll-animate">
    <h2 class="mb-6 text-white">What's In The Report</h2>
    <p class="text-gray-300 mb-6">
      Your audit report is typically 5-10 pages of specific, actionable information—no fluff. You'll receive:
    </p>
    <ul class="space-y-4 text-gray-300">
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
        </svg>
        <span><strong class="text-white">Current State Assessment</strong> — What's working, what's not, and why</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
        </svg>
        <span><strong class="text-white">Specific Recommendations</strong> — Concrete changes with expected impact</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
        </svg>
        <span><strong class="text-white">Priority Action List</strong> — Numbered list of what to fix first, second, third</span>
      </li>
      <li class="flex items-start gap-3">
        <svg class="w-5 h-5 text-accent-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
        </svg>
        <span><strong class="text-white">Tool Recommendations</strong> — Specific tools or features to implement</span>
      </li>
    </ul>
  </section>

  <!-- Upgrade Path -->
  <section class="mb-12 scroll-animate">
    <h2 class="mb-4">Ready for More?</h2>
    <p class="text-gray-300">
      If after your audit you decide you want more hands-on help, your $500 audit investment can be credited toward <a href="/services/vip-strategy-day/" class="text-accent-400 hover:text-accent-300 font-medium">VIP Strategy Day</a> or <a href="/services/done-for-you-systems/" class="text-accent-400 hover:text-accent-300 font-medium">Done-For-You Systems</a> within 30 days.
    </p>
  </section>
</ServiceLayout>
